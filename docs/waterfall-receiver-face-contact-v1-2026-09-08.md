# Waterfall on the receiving side of its shore step

8 September2026. Accepted bounded Design/Asset presentation correction, implementation pending. PM requested resolution from Engineering ded9889d's existing source/fixture/projection diagnosis. This explicitly supersedes the earlier geometry-unchanged restriction only for the matched shore-step overlap and narrow lip contact described here. It changes no water generation, source connectivity, logical elevation, movement, save facts or disclosure. Installed360 remains the current delivery.

## Evidence and exact relationship

Only generated-waterfall time-animates. Large cyan regions are static horizontal water planes; blue bed plates and shore surfaces are separate. The retained source/receiver edge(2,2)→(2,3) has surfacequarters7→3, beds5→1. Curtain center(0,.625,-.5), size(.35,.5,.025), spansY.375… .875. The same admitted shore step center(0,.375,-.5), size(.97,.5,.03), spansY.125… .625. Its opaque face lies in front of the centered curtain and hides its lower half. The34×26px changing patch is the exposed portion, not the full falling face. Shader motion is active; another contrast sweep does not resolve this contact defect.

Water falls on the downstream/receiver side of that riser. Its placement must follow source→receiver in world coordinates, never the camera. Normal depth testing and legitimate occlusion remain; a fall directed away from the camera need not expose its full face.

## Final placement authority

Match the exact unordered pair of cells between the admitted water connection and its sanitized measured shore STEP. Require actual positive vertical overlap between current curtain and riser. Do not match nearby steps, slopes or arbitrary brown geometry. With no matching overlapping step, preserve the current curtain placement.

Let n be the normalized horizontal cardinal source→receiver direction. Preserve curtain width .35, thickness .025 and exact source/receiver surface-derived upper/lowerY. Place its upstream face .0025scene unit beyond the step's actual downstream face. Derive the center offset from actual faces/half-thicknesses. For the current shared center: .015stepHalf + .0125curtainHalf + .0025clearance = **.030tile downstream offset**. Both surface planes and all step geometry retain their current positions.

This final .030/.0025 pair supersedes the crossed-message provisional .0285/.001 pair; there is one implementation authority. The clearance is routine Design/Asset presentation tuning, not an additional Aimee requirement.

Close a resulting upper contact gap with only a same-width horizontal lip strip at the exact source surface height, from the actual source water plane's matching edge to the relocated curtain's upstream top edge. Current .97tile source plane ends .015tile upstream of the shared edge; shifted sheet begins .0175downstream, so the join spans .0325tile. It includes the actual admitted lip, adds no lateral width and overlaps no unrelated region. Use static source-water appearance on the join; only the falling curtain has the waterfall time shader. No whole-plane resizing, new pool, spray, apron, animated bed/shore or invented fluid motion. Receiver water already extends under the shifted sheet's lower edge; retain the exact receiver landing height.

Generate sheet and join only from the same admitted, currently disclosed positive-drop connection and endpoint surfaces. Their lifecycle is shared: withheld/removed/invalid connection removes both. Remembered or hidden state does not reveal a live connection, surface or receiver. No always-on-top/depth-disable/camera offset, transparent terrain, cut-out step or new crossing permission. This is transient presentation of existing saved facts, not a map mutation or regeneration policy.

## Material and bounded verification

Retain Asset's already specified bad5f803 contrast constants and existing base/alpha.80, roughness1/specular0, three finite lanes, .30tile repeat, .45tile/s downward phase and derivative filtering. Preserve fallback behavior. This correction grants no contrast sweep or new water shader family. Current visual NON-PASS9a27170a remains until affected evidence passes.

Engineering checks exact edge match, downstream sign and face clearance, unchanged width/upper/lowerY, no-match behavior, unchanged logical/saved facts and sheet/join removal under withheld endpoints. Extend existing geometry/disclosure checks, not a new world corpus or gameplay sweep. One affected retained native sequence at existing ordinary scale and actual target/default/current appearance establishes whether the exposed falling face reads as descending water and remains continuously connected without spill or z-fighting. Asset reviews that same evidence; no separate diagnostic capture, Design recapture or camera/configuration matrix. Native proof must distinguish curtain from static water context, and clip-loop splice from shader period. Natural generated-waterfall occurrence and physical-phone play acceptance remain separate unverified claims.

Engineering and Asset have this final relationship; PM receives the published record. Other ready production continues independently if this local correction encounters a concrete blocker.
