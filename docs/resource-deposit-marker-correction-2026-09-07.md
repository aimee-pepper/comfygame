# Resource deposit marker correction

> **New direct Aimee instruction supersedes every future2D mapping authorization below.** Use [Temporary2D deposit filter](temporary-2d-deposit-filter-v1.md): new ordinary2D worlds exclude unfinished box deposits; existing worlds/stock remain. The temporary ore→Iron mapping is withdrawn and never counts as finished art approval.3D node graphics take priority. The remainder is historical decision context, not an active assignment.

7 September2026. Design resolution of Engineering's exact341 report. **SUPERSEDED before delivery: no further2D asset work.** In-world crash investigation is separate urgent Engineering work; this artwork mapping does not fix or explain the crash.

Current source: `Sources/Screens/WorldView.swift` TileView renders early blocking deposits through generic `PhysicalMaterialID.icon`/cube.fill. Actual reported sources include Coal, Iron and Quartz. Existing packed Pick/tier requirements still come from the real source definition. Counts, private campaign identifiers and crash diagnostics stay in Engineering records, not player explanations.

| Actual source | Authorized bounded map presentation |
| --- | --- |
| Coal | Use the existing Aimee-selected original `RuntimePacks/EarlyResourceOriginals-v1/coal.png`, respecting the native map consumer's sizing and disclosure. No redraw/recolour. |
| Quartz | Use its own existing legacy resource sprite through the current appropriate map/field rendition. |
| Iron | Design authorizes the existing legacy `ore` sprite as a **temporary visual stand-in only** in this exact marker consumer. Source name remains Iron in selection/inspection and tool requirements remain source-owned. It is not final literal Iron artwork or an Aimee-selected original. |

Design inspected existing legacy ore field/inventory images in `AssetLab/integration/resource-sprites-v1`; the rock/vein illustration is adequate for this provisional representation. Engineering uses the native consumer's existing correct rendition/lookup, not a speculative export family. Reusing a picture never aliases legacy ore inventory/resource IDs to Iron, converts resources, changes provenance/material colour or adds a fake personally approved original to `PhysicalMaterialInventoryArtwork.approvedImageNames`.

Keep full-current-sight and source matching, occupancy/base ownership, depletion, target priority, tools/tiers and world turns unchanged. Unknown/remembered material cannot be revealed by the image. Failed artwork lookup keeps a clear source-appropriate functional fallback with truthful Iron selection/inspection naming, not an unexplained generic cube presented as final art. Do not repurpose quality colours as material identity or imply a new emitted light/hazard.

Existing Asset truncation correction remains its separate exact implemented UI task; do not add a new layout survey. Engineering verifies the actual native marker consumer once with the source/name/tool relation and integrates the crash correction independently. No new world search, resource count census, texture generation, physical-phone rerun by Design or final-art claim. PM receives blockers requiring Aimee engagement.
