# Only notifications move — direct Aimee correction

8 September2026. **Urgent accepted correction; not delivered.** Aimee's exact instruction relayed by PM is **“JUST THE NOTIFS NEED TO MOVE”.** It supersedes PM's moving-navigation interpretation AND the intermediate information/notification-pane scope. No new question or interpretation expansion.

## Sole moving owner

Only the transient event/narration/notification pane moves within the MAP viewport. Its own Read all expansion and exact Diary Read now action belong to it. At this place/context/source-action pane and its details stay fixed at the original bottom-map position. Never move the whole WorldFieldFeedbackRow.

Restore carriedStrip and controls(run) to their original pre359 BELOW-map sibling layout: satchel/Field Kit/turn strip above fixed D-pad, minimap, Use Tile and Look. Restore their original viewport reservation once. Headers/party/Seamward, loot, tutorial and readers retain their original owners/positions. Notification flips never resize/reframe/zoom the restored map. No new mode, preference, world, camera behavior or broad rollback.

## Geometry and opposite-edge hysteresis

Use existing projected full actor bounds A and tile height T in one coherent clipped-map layout revision. Only the actual painted/hittable notification rectangle and its own expanded content supply a proximity trigger. Missing notice or transparent gutter supplies none. Fixed context and every other pane are excluded from the moving set and trigger.

With horizontal overlap: bottom clearance=notice.minY−A.maxY; top clearance=A.minY−notice.maxY. At clearance≤2T switch only if the opposite candidate is clear of its own approach zone. Retain through the middle. Consume each transition observation and rearm only after a later clear snapshot at the new anchor. Both-unsafe retains deterministically; no oscillation. Missing/stale geometry retains the current anchor. Transient run-owned state starts bottom with one valid initial evaluation, no saved preference.

Keep notification width/alignment; independently place it above/below without dragging context. Its expanded content opens inward with existing capped scrolling. Fixed context details retain their upward expansion. Respect fixed context/details, loot and tutorial occupied rectangles and existing priority without moving them. Constrain/scroll notification within available space; an actual impossible measured constraint goes to PM/Engineering, never broadens the moving set.

## Identity and input

Preserve exact event batch/ordinal/page, queue order, expiry and notice preferences. Moving cannot restart a timer, acknowledge another event, choose a different Library page or regrant rewards. Open diary reader survives expiry and returns to the same Field.

The one-time layout restoration cancels old touches/timers whose frames move. Subsequently a notification flip cancels only held notification controls and map gestures newly covered/moved by it. Fixed controls keep their stable identities/admission; do not cancel D-pad or Use Tile merely because a notice moves. Existing direction actions occur on release; add no repeat timer. Committed actions stay committed, old release cannot activate a different target, fresh notification input uses current frames.

## Priority and acceptance

Restore urgently, before sidewalls. Preserve361 water,360 plants,358 diary,357 normal3D and all campaigns. Sidewall/shadow/real-art/gathering-restoration work stays authorized; preserve the creature-disclosure checkpoint.

Reuse current tests and one affected actual-target/default/current native route: notification at both edges while context and ALL fixed-control frames stay unchanged, restored map reservation/tile scale, exact Read now/expiry and safe old release/map touch. Asset reviews the same evidence; no Design capture, new trial or phone-launch gate. Earlier359 whole-block visual/technical PASS is historical evidence of rejected scope, not acceptance. Wiki must state current regression and intended correction separately until delivery.
