# Encounter Scaling — First Three DEBUG Candidate Profiles

**Status:** historical DEBUG comparison set. The ordinary missing-foe columns and upper-median party
reference were rejected by the 11 August design audit. Profile B's apex coefficients informed the
current additive-party-power candidate, but this table is not an alternative implementation
authority. The sole current rules handoff is `encounter-scaling-playtest-current.md`.

These historical profiles shared full active-party input, maximum three actual visible persisted
foes, ordinary Stability/greed contribution applied once, deterministic persistence, opt-in
stationary apex and unchanged Unbind. Their **upper-median** reference and hidden ordinary `+level`
conversion are not settled boundaries and must not be copied into a new consumer.

| Parameter | A — Reserved | B — Recommended start | C — Pressing |
|---|---:|---:|---:|
| Ordinary equivalents per extra party member | 0.35 | 0.50 | 0.65 |
| Implemented missing-visible-foe conversion cap *(superseded candidate)* | +1 level | +2 levels | +2 levels |
| Implemented half/remainder strength step *(superseded candidate)* | +1 when stable roll fires | +1 when stable roll fires | +1 whenever remainder ≥0.35 |
| Apex level offset above party upper median | +1 | +2 | +3 |
| Apex HP per extra member | +0.25 | +0.35 | +0.45 |
| Apex HP multiplier cap | 2.0× | 2.4× | 2.8× |
| Apex offence per extra member | +0.06 | +0.10 | +0.12 |
| Apex offence multiplier cap | 1.25× | 1.4× | 1.5× |
| Apex action slots for party 1–2 / 3–4 / 5 | 1 / 2 / 2 | 1 / 2 / 3 | 1 / 2 / 3 |

Profile B supplied the equal-level apex targets retained by the current candidate: +2 floor, 2.4× HP,
1.4× offence and three action slots at five people. The current rules derive the HP/offence values
from additive party power rather than raw member count and derive the level floor from the Binder
anchor, as specified in the playtest authority. Profile C remains historical upper-bound evidence;
do not continue comparing the ordinary +level rows as if they solve action-economy scaling.

## Historical fixture matrix

The original comparison requested one deterministic result per profile for party counts 1, 2, 3 and
5 at level bands 1, 8 and 16. Preserve any existing exports as history; do not build new acceptance
around these obsolete assertions:

- one isolated ordinary foe, proving missing budget becomes a bounded visible adjustment;
- three adjacent awake ordinary foes, proving no fourth foe appears and no double danger applies;
- one apex, reporting level, HP, offence and explicit round action schedule;
- one uneven party (`2,4,6,8,20` at five members), which historically proved upper median 6 but does
  not prove the current additive model;
- relaunch equality for entity IDs, level adjustments, remainder roll and apex schedule.

The current phone matrix, transparent pressure diagnostics and promotion rule are exclusively in
`encounter-scaling-playtest-current.md`. A/B/C may remain selectable in DEBUG for historical
comparison, but passing their old fixtures cannot promote their level reference or ordinary spend.

## Promotion rule

After device comparison, record any coefficient revision in a new dated balance decision. Preserve
this file and exported results as history. Do not relabel Recommended “final” without the current
comparison, and do not change creature trait generation to make a combat profile look successful.
