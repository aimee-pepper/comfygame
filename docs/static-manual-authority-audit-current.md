# Static/manual authority audit — current

**Status:** active audit; fourth repository pass 11 Aug 2026
**Owner:** Game Design classifies authority; Engineering automates only approved cases
**Roadmap ID:** `dynamic-authority-audit`

## Boundary

“Dynamic” does not mean procedural or remotely configurable. Authored prose, balance constants,
stable IDs, visual identity mappings and deliberate campaign order remain authored. This audit finds
facts copied into two or more places where one copy can change without the others, and recurring
status chores that can be derived from evidence already in the repository.

Classify each finding as:

- **derive** — one canonical source already exists; consumers should read or generate from it;
- **validate** — two representations are legitimately required, but a test must prove agreement;
- **author** — the apparent duplication is an intentional design decision and remains explicit;
- **archive** — historical text remains readable but must stop presenting itself as current status.

## First-pass findings

| Priority | Area | Drift risk | Direction |
|---|---|---|---|
| Closed in `71ffca2`; duplicate literal removed 12 Aug | Native combat-v2 generated catalogue | Native content previously lagged schema-2 topology, canonical technique ownership and Effect-copy linkage; a Swift test later repeated the generated Effect hash by hand and became the sole stale full-suite fixture after a legitimate copy regeneration | **Generate/validate:** native model and validator consume canonical optional `techniqueID` and exact Effect copy while retaining legacy payloads only for migration. Mandatory `python3 scripts/generate_combat_tree_v2.py --check` owns the authority/copy/source hash chain; Swift tests cover 72 IDs/parents, exactly 20 technique IDs/52 nulls, exact Effects and topology without pinning a second handwritten generated SHA. Preserve the check in ordinary verification; immutable asset/golden hashes remain pinned because they intentionally detect visual-contract changes |
| Closed | DEBUG Roadmap | Swift UI and Markdown previously repeated operational status | **Derive:** the app now renders bundled `playability-roadmap.json`; Markdown owns rationale and checkpoint history only |
| Partly closed / P2 validation | Engineering question lifecycle | `engineering-questions-for-aimee.md` retained resolved Noll/ink/combat/refining/Token/minimap decisions under Open and reused `EQ13`, allowing retired mechanics to re-enter as apparent blockers | **Author/validate:** resolved entries move to the preserved Resolved archive with deciding source/date; headings use unique stable EQ IDs. The 11 Aug pass reconciled EQ2/13/26/28/29/30 and renumbered opening safety EQ32. Add a lightweight heading-uniqueness check; status judgment remains authored |
| P1 active / exact handoff ready | Roadmap header/workstream mirrors | `playability-roadmap.json` still manually stores `currentWork`, `currentNote` and `installedCheckpoint`, and its test requires exactly one global `inProgress` item. The 11 Aug Field Controls checkpoint produced the predicted bookkeeping-only failure while World Color legitimately remained `readyToTest` | **Derive/split:** implement `debug-roadmap-derived-workstreams-current.md`: each item owns stable workstream/primary/status/detail; allow at most one primary per workstream; derive headers from concurrent streams and actual bundle build metadata. Task status remains authored JSON |
| P0 active | Save ownership | One implicit save path made campaign identity and current game inseparable | **Derive:** UUID slot envelope owns metadata plus payload; campaign chooser reads the slot catalogue; legacy file is adopted once |
| P1 | Installed checkpoint context | Bug reports previously required a person to remember which roadmap/build state they described | **Derive:** every new report captures the bundled roadmap checkpoint plus app build; old reports decode without the field |
| P2 deferred after launch-scope audit | Installed binary provenance and launch-cache identity | Every locally installed build reported marketing version `1.0`, bundle version `1`, and no source revision. After installation, neither Aimee nor Engineering could prove which commit produced the observed black launch, and iOS could plausibly retain an obsolete launch snapshot under the unchanged identity | **Record now; derive later:** bump the bundle version once for the launch-cache acceptance install and record its clean commit/build in the install receipt. A first attempt at generated signed provenance repeatedly blocked the build and was cut before it displaced the visible launch repair. Revisit generated source revision/dirty state with the roadmap/build-metadata authority work; never mutate Xcode's processed Info.plist or weaken signing |
| P1 | Station presentation order | `stations.json` contains repeated `sortOrder` values; current secondary order is merely JSON array order and the Swift comparator has no explicit tie-break | **Author/validate:** adopt `homeSection` + unique `sectionOrder` from `base-destination-board-current.md`; validate the complete mapping and array-shuffle invariance. Legacy ID mapping is decode compatibility only |
| Partly closed / P1 validation | Authored-text atlas census | The original 28-live/7-meeting milestone is obsolete now that Noll is the twenty-ninth live traveller with a provisional eighth meeting; hard-coded milestone assertions can still drift | **Derive/validate:** atlas rows are the union of live catalogue IDs and generated review-corpus identities; live missing, side-by-side revisions and replacement candidates derive by set relation. Current diagnostics reconcile 29 live / 8 live meetings / 23 review objects; missing metadata fails validation |
| Source-complete 12 Aug / normal freshness check still P1 | Draft-meeting availability | `AuthoredTextAtlas.draftMeetingIDs` manually repeated the generated review-corpus IDs; adding/removing a draft could make the atlas label it Missing or omit its review state | **Derived:** atlas availability now consumes the stable traveller-ID set directly from generated `DraftMeetingCorpus`, with a relation test against the 29-person catalogue. `Scripts/generate_draft_meeting_corpus.py --check` is green; adding that check to ordinary verification remains the final process step |
| P1 | Generated meeting corpus | Markdown drafts are compiled into `DraftMeetingCorpus.generated.swift`; the generator exists, but ordinary build correctness depends on someone rerunning it | **Validate/generate:** add a check mode to the normal verification path that fails when generated output differs from its Markdown sources; never make runtime parse design Markdown |
| P1 | Resource identity surfaces | Map and collection UI previously had separate resource symbols, allowing accepted v0.6 identities to degrade to generic icons | **Derive:** one native resource grammar now feeds map and collection surfaces, with the inventory-only Mote exception fixture-covered |
| P1 | Item identity surfaces | Catalogue SF Symbols collapse multiple boots, keepsakes, tools and weapon families into identical six-across tiles | **Author once/derive:** Aimee authors/approves the versioned handmade item pack; generated adapters derive the stable mapping; Storehouse, Trading Post, loot, equipment and Recycler consume the same resolved identity; AssetLab validates only |
| P1 | Trading Post trade metadata | `TradingPostRules` manually owns a 23-resource band map plus separate transferable/nontransferable item-ID lists even though resources/items are growing JSON catalogues. Its completeness check prevents silent access but every addition still requires a Swift allowlist edit and presentation cannot inspect the authored reason | **Author once/derive:** add explicit versioned `tradeBand` to resource definitions and `tradingPostDisposition` (`sellable`, `protected`, `notBought`) to item definitions; rules, stock pools, sale UI and audit derive from it. Missing data is a catalogue validation failure, never rarity/name inference |
| P1 | Recycler salvage metadata | `RecyclerRules` explicitly maps every current ordinary gear ID to an honest versioned profile and validates completeness, but the private Swift table duplicates the growing item catalogue; a new gear entry requires remembering the second authority | **Author once/derive:** add `salvageProfileID` or explicit protected disposition to item metadata and keep profile output sequences in one validated registry. Recycler derives eligibility/profile; missing metadata fails sell-only. Preserve all current profile IDs/outputs and real-receipt precedence |
| P1 implementation-ready | Expedition return receipt construction | Full-haul and partial-haul exit paths separately construct the same `RunExitSummary` field list. Adding pages/travellers/writing/XP/economy context to one path but not the other can recreate the exact “collected but absent from recap” defect even though the underlying Reality data is correct | **Derive:** use `expedition-return-receipt-current.md`; one rules-owned `makeReturnReceipt(run, banked, outcome, state)` freezes every recap category from departure baselines and the atomic bank result. Portal/Waystone/collapse/defeat/abandon supply only their genuine outcome differences. A field-completeness fixture runs the same seeded discoveries through full and partial exits and compares every non-loss-dependent category |
| P1 correctness before failure-agency UI | Discrete partial-haul retention | `Inventory.randomlyKeeping` accepts but never consumes RNG and floors each stack separately, so four distinct one-off finds retain zero at 50%/75% while four identical stacked items retain two/three | **Derive:** compute one total-unit budget over stable at-risk units, independent of catalogue bins/stack splits; use an outcome-ID-keyed deterministic suggestion if automatic selection remains. Player-editable recovery stays a separate DRQ-089 review choice |
| P2 | Future qualifier vocabulary ownership | Source words have authored acquisition and permanent ownership, but qualifiers are currently globally writable and their `step` field assumes every family is ordinal. Any future learned non-ordinal qualifier family would otherwise require screen filters, save grants and found-writing rewards to repeat special cases. Colored ink no longer depends on this work | **Author once/derive if such a track ships:** `QualifierDef.acquisition` and `BaseState.ownedQualifiers` own writability; typed qualifier teachings grant that set; palette/Library/summary derive from it. Ink ownership instead derives from the Scriptorium Ink mixing upgrade |
| P1 | Traveller identity surfaces | Party and Library use generic person/catalogue symbols despite accepted named-character identity work | **Derive after adapter:** one persisted character identity/facing adapter should feed map, Party and Library; no screen-local surrogate portraits |
| P1 | Human visual-origin completeness | The accepted character adapter can resolve named travellers and proof-only generated seeds, while Binder and Quill still require fallbacks and `traveller == nil` remains an accidental identity signal | **Author once/derive:** persist the closed Binder/Quill/named/generated union from `binder-quill-generated-visual-identity-current.md`; every map/Party/Library/combat consumer resolves through it. Missing cases diagnose rather than borrowing a person, seed or roster position |
| P1 | Combat-node glyph mapping | The 72 stable node IDs and semantics are machine-readable/current, but their authored pictorial referents currently live only in a Markdown table and could be retyped differently into a future handmade/native registry | **Author once/validate:** Aimee supplies or approves one exact-ID handmade glyph pack; its registry is generated from or checked against `combat-tree-v2-authority.json`. AssetLab may validate placeholder/pack completeness, unknown IDs, duplicate hashes/collision groups and cross-surface identity, but does not author the final contours. Release never parses Markdown |
| P1 | Distillery requirement copy | `StationViews` manually repeated Essence, catalyst and material thresholds separately from `DistilleryRules`, so a tuning change could make visible requirements lie | **Derive:** one structured rules-owned requirement now drives candidate eligibility, catalyst options and visible requirement text; focused boundary tests protect the shared thresholds |
| P1 | Anchor Frame recipe copy | Anchorage UI manually repeated four property thresholds, multiplicities and the 60-Essence cost separately from `AnchorFrameRules` | **Derive:** rules now expose ordered grouped needs; recipe selection, shortfall and visible rows/cost share the same authority, with a multiplicity fixture |
| Closed / phone acceptance | AssetLab→native render bridge | The accepted world-grade-2 manifest is pinned and native conformance is green at `57d8850`; the bind adapter now produces an immutable receipt consumed by native rendering and History | **Validate/derive:** preserve pack conformance and receipt/hash fixtures; repeat this manifest→conformance→receipt pattern for later generated-asset families rather than hand-porting appearance rules |
| Closed / phone acceptance | World visual receipt | Material, Smoke, flora and emitter facts previously risked screen-local re-derivation and historical recoloring | **Derive once/persist:** newly bound worlds now freeze the versioned `WorldVisualReceipt`; legacy receipt-less worlds remain world-grade 1. The 1,057-test checkpoint covers canonical self-hash, atomic bind, non-chasm realized flora, History/relaunch, fog/resources and renderer consumption; installed-phone visual acceptance remains |
| P2 | Growing permanent capability flags | Chaining is a dedicated `BaseState` Boolean and each future Scriptorium capability would invite another field, coding key, migration, grant switch and screen check | **Derive/migrate:** use a stable `Set<CapabilityID>` granted from research content; decode old booleans into the set once. Keep typed conveniences as computed accessors, not writable authorities. Do this when Ink Mixing or Compound Assembly first enters native schema, not as unrelated churn |
| P1 queued | Penmanship identity and graph | `pen_pencil` is repeated across research JSON, diary leads, tests and current model display while current design is Brush; the same catalogue also encodes an obsolete line and has no Ink Mixing/Compound Assembly nodes | **Author once/migrate:** `penmanship-native-migration-current.md` owns the new stable IDs and DAG. Catalogue prerequisites drive graph/UI; one decode alias migrates `pen_pencil` completion/lead state to `pen_brush`; `Hand.plain` retains serialized geometry but derives display Brush; capability grants feed the shared capability set |
| P2 | Combat affliction vocabulary | `StatusKind`, string-keyed tuning dictionaries, coating→status mapping, cure eligibility, tick copy and UI glyph treatment each repeat parts of Burn/Poison/Dazzle plus legacy Bleed. A new producer or rename can therefore exist mechanically while remaining untuned, incurable or visually mislabelled | **Author once/validate:** keep effect application explicit, but give each stable status one typed definition owning current display/glyph, default tuning key, persistence role and cure families. Coatings and emanations reference stable status IDs; validation proves every producible status has tuning, presentation and at least its intended removal boundary. Legacy Bleed remains an explicitly marked compatibility status rather than being inferred as Poison |
| Partly closed / promotion follow-up | Combat technique ownership | `ce9b1af` replaced the universal human grant with one typed resolver for Binder, roster-stable Quill, Ashe and graph nodes, but `skills.json` still retains old two-person `binder`/`companion` owner fields and future Sight/Read instruments are not yet live sources | **Derive/migrate:** preserve the implemented identity split; at combat-v2 catalogue promotion, make legacy catalogue owner decode-only and consume canonical identity, authored-person, stable graph-node and exact instrument/item sources through the same resolver. DRQ-160's later feel review does not block consumers or authorize universal fallback |
| Partly closed / native migration queued | Active-node grant authority | Current native v2 content stores only legacy technique payloads: it omits active Blur and retains Quench→Steady/Emanation Strike→Elemental Strike | **Author once/validate:** schema-v2 `combat-tree-v2-authority.json` now owns the exact 20-entry `techniqueIDByNode` map and Asset glyph consumers derive from it. Native promotion must materialize the same optional stable `techniqueID` in canonical game content, validate exact 20 non-null/52 null coverage, and keep legacy IDs decode-only |
| Closed / native consumption queued | Combat node Effect copy | Exact current behavior for 72 nodes was authored in Markdown while native v2 retained obsolete poetic blurbs, requiring a future manual transcription | **Generate/validate:** `combat-tree-node-copy-current.md` remains authored copy; `combat-tree-v2-effect-copy.generated.json` supplies exact stable-node strings plus source/authority hashes. Normal AssetLab verification fails if stale; native promotion consumes the artifact and rules-owned substitutions rather than parsing Markdown |
| P2 | Current-document indexing | `current-design-index.md` is a manually curated router and several old “current” documents retain stale live-status paragraphs | **Validate/archive:** add lightweight structured metadata or a manifest for current/superseded/owner/roadmap IDs and generate a drift report; keep prose and decision history authored |
| Partly closed / P2 systemic | Design-document priority headers | `debug-bug-reporting-current.md` still declared relay delivery the active checkpoint after the live roadmap had moved through later work and reopened launch, creating a second executable-looking queue in prose | **Derive/archive:** corrected that document to name `playability-roadmap.json` as queue authority and retain only feature status/rationale. During ordinary doc edits, replace `active/next/current priority` claims with the stable roadmap ID unless the file is itself the canonical active item. A later document-status validator should flag operational priority language in non-roadmap current docs rather than requiring periodic manual recensus |
| Archived 11 Aug | Phase-1 acceptance/priority snapshots | `phase-1-phone-acceptance-card-current.md` and `playability-priority-sequence-current.md` still presented `ce9b1af`, pending Noll and Spring source work as current after those checkpoints moved, making an old protected-phone plan look executable | **Archive, do not refresh:** both now preserve their capture-time sequence with an explicit historical warning and route live status/provenance to the roadmap/install receipt. Reusable acceptance criteria may be linked by a live item; the snapshot itself is never manually advanced |
| Partly closed | State-of-build status prose | The 9 Aug overview still called legacy linear combat “complete,” copied an obsolete test total and exposed its old immediate queue beneath a warning that it no longer owned scheduling | **Archive/derive:** top overview now states current gaps and points to the operational JSON; historical queue is explicitly non-executable. Future build/test/active-work values derive from bundle/roadmap evidence rather than manual prose refresh |
| P2 | Exact catalogue counts in tests | Some raw counts are useful release-scope assertions; others are accidental edit taxes | **Classify:** replace accidental totals with set/reference invariants, retain explicit scope caps only when the number itself is a settled design promise |
| P2 | Data-to-route bridge | Station route strings and compiled `AppRoute` cases must both exist | **Validate, do not derive:** views are compiled code; keep the existing routing completeness test and improve its error to name missing/orphan routes |

## First implementation order

1. **Closed in `71ffca2`:** native schema-2 generation, canonical technique IDs, exact Effect copy,
   authority hashes and the normal freshness check are green. Preserve this gate while implementing
   remaining consumers; do not regenerate or migrate it again as a substitute for consumer work.
2. Finish and test save-slot ownership; it removes the highest-cost manual test-state bottleneck.
3. Keep roadmap checkpoint/build context attached automatically to bug reports.
4. Remove Roadmap header mirrors: current work derives from active items grouped by workstream/owner,
   and installed build derives from build metadata rather than a post-install JSON edit. Replace the
   exactly-one-global-in-progress test with unique stable IDs and at-most-one-primary-per-workstream.
5. Make generated meeting-corpus drift a normal verification failure and derive draft IDs from the
   generated corpus rather than a second Atlas list.
6. Move Trading Post disposition/bands and Recycler salvage dispositions into catalogue metadata
   before the next large resource/item expansion; keep missing classification fail-closed.
7. Resolve station ordering before another station exposes incidental ties.
8. Convert atlas completeness from arithmetic mirrors to catalogue-ID set comparison while retaining
   named corpus milestone snapshots separately.
9. Preserve the now-green world-grade-2 manifest→native conformance→receipt bridge and complete its
   installed-phone visual acceptance; use the same pattern before another asset family requires a
   hand port.
10. Introduce a capability set with the first native Ink Mixing/Compound Assembly schema rather than
   adding more permanent booleans.
11. Introduce a structured document-status manifest only after the live gameplay blockers above; do
   not bulk-edit decision history merely to satisfy a tool.
12. Consolidate the expedition return-receipt builder before adding another recap category or exit
    type; this is a correctness seam, not a visual refactor.
13. Add the typed affliction-definition validation alongside the combat-v2 consumer work, when those
    status consumers are already being touched.
14. Keep the 72-node placeholder manifest functional and validate its exact ID set against graph
    authority; replace central marks only from Aimee's later handmade pack, never an Asset-authored
    semantic candidate or third native hand map.
15. Add the closed human visual-origin union alongside the stable party-member identity migration,
    before native character assets make Binder/Quill fallbacks look like accepted identity.
16. Preserve the `ce9b1af` identity resolver during combat-v2 ownership promotion; migrate catalogue
    owner strings to decode-only and add exact instrument/item sources without creating a parallel
    authority. DRQ-160's later Quill-free-healing feel review is not an implementation prerequisite.
17. Materialize the exact 20-action grant registry in canonical combat-v2 content before the graph
    becomes writable; generated/runtime consumers must not infer Blur or preserve stale skill IDs.
18. The first consumable identity seam now has one generated functional-placeholder pack at
    `AssetLab/integration/catalogue-consumables-placeholder-v1/manifest.json`: 17 exact assets plus
    61 explicit fallbacks partition the 78 live IDs and pin commands/RGBA. Generate the native
    registry from this pack rather than copying rectangles by hand; retain its `finalArt: false`
    status until Aimee supplies the replacement `catalogue-items-v1` pack. Apothecary and carried-
    remedy/Field-Kit consumers must join by stable catalogue ID rather than keep separate SF-symbol
    identity paths.
19. The named-character compact/map seam now has one generated functional-placeholder pack at
    `AssetLab/integration/named-character-placeholders-v1/manifest.json`: all 29 live TravellerIDs,
    one explicit compact cameo and four top-down facings each, with exact command/RGBA hashes. Party
    and Library must share the cameo profile; World must use only the top-down profile. Generate one
    registry from this pack instead of maintaining SF-symbol and map identity paths separately.
    Preserve `finalArt: false`, Noll's working-placeholder provenance and Aimee's replacement
    authority; Binder, Quill and generated-person origins remain a separate schema gate.

## Acceptance for the audit itself

- Every candidate names its canonical authority and all known consumers.
- Automation does not erase authored judgment or make old decisions disappear.
- A changed canonical source either updates consumers automatically or fails verification with a
  precise message.
- No dynamic/runtime dependency on repository Markdown is introduced into Release builds.
- The live roadmap tracks implementation status; this document tracks findings and rationale, not a
  second operational board.
