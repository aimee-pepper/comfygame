import assert from "node:assert/strict";import fs from "node:fs";import path from "node:path";import {spawnSync} from "node:child_process";import {PAGE,MARGIN,WRITING,SOCKET,guideCoordinates} from "../src/writing-parchment-v1.js";
const root=path.resolve(import.meta.dirname,".."),out=path.join(root,"artifacts/writing-parchment-v1"),run=()=>spawnSync(process.execPath,[path.join(root,"scripts/export-writing-parchment-v1.mjs")],{encoding:"utf8"});let r=run();assert.equal(r.status,0,r.stderr);const one=fs.readFileSync(path.join(out,"manifest.json"));r=run();assert.equal(r.status,0,r.stderr);assert.deepEqual(fs.readFileSync(path.join(out,"manifest.json")),one);const m=JSON.parse(one);assert.equal(m.integrationReady,false);assert.equal(m.status,"final-art-candidate-review-v0.2");assert.equal(m.productionSource.referenceProductionSource,false);assert.equal(m.runtime.width,PAGE);assert.equal(m.runtime.height,PAGE);assert.deepEqual(m.runtime.writingArea,{x:MARGIN,y:MARGIN,width:WRITING,height:WRITING});assert.deepEqual(m.runtime.socket,{columns:6,rows:6,width:SOCKET,height:SOCKET});assert.deepEqual(m.runtime.guideCoordinates,guideCoordinates);assert.ok(m.runtime.paletteCount<=24);assert.deepEqual(m.runtime.alphaValues,[0,255]);assert.equal(m.constraints.runesChanged,false);assert.equal(m.constraints.uiChanged,false);assert.equal(m.constraints.gameplayChanged,false);for(const name of [m.runtime.path,...m.evidence.map(x=>`evidence/${x}`)])assert.ok(fs.existsSync(path.join(out,name)),name);console.log(`writing parchment v1 passed ${m.canonicalBodySHA256}`);
