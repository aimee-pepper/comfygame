export const combatUIVersion="combat-ui-proof-0.1.0";
export const conditionOrder=Object.freeze(["burn","poison","dazzle","ground"]);
export const voiceOverOrder=Object.freeze(["encounter","currentActor","action","target","party","foes"]);
const member=(id,name,side,hp,maxHP,armour,statuses=[],extra={})=>({id,name,side,hp,maxHP,armour,statuses,passedOut:false,...extra});
export function combatUIFixture(){return{schemaVersion:1,uiVersion:combatUIVersion,encounter:"Buried-seam crossing",currentActorID:"party-a",action:{kind:"gambit",name:"Brace and draw",targetID:"foe-b"},members:[member("party-a","Mara","party",31,40,8,["ground"]),member("party-b","Iven","party",18,28,3,["burn"]),member("party-c","Sable","party",0,34,5,[],{passedOut:true}),member("party-d","Orsa","party",22,22,1,["dazzle"]),member("party-e","Wren","party",26,30,4,["poison"]),member("foe-a","Plate grazer","foe",24,38,9),member("foe-b","Unknown form","foe",17,25,2,["ground"],{unknown:true}),member("foe-c","Triggered flora","foe",12,18,0,["burn"])]};}
export function validateCombatUI(fixture){const issues=[],ids=new Set(fixture.members.map(member=>member.id));if(!ids.has(fixture.currentActorID))issues.push("current actor missing");if(!ids.has(fixture.action.targetID))issues.push("target missing");if(fixture.action.kind!=="gambit")issues.push("proof must include gambit turn");for(const member of fixture.members){if(member.hp<0||member.hp>member.maxHP)issues.push(`invalid HP ${member.id}`);if(member.armour<0)issues.push(`invalid armour ${member.id}`);if(member.passedOut&&member.hp!==0)issues.push(`passed-out actor has HP ${member.id}`);for(const status of member.statuses)if(!conditionOrder.includes(status))issues.push(`unknown condition ${status}`);}return issues;}
export function conditionLabel(value){return value==="ground"?"Ground":value[0].toUpperCase()+value.slice(1);}
export function accessibilityLabel(member){const health=member.passedOut?"passed out":`${member.hp} of ${member.maxHP} health`,armour=`${member.armour} armour`,conditions=member.statuses.length?member.statuses.map(conditionLabel).join(", "):"no conditions";return`${member.name}, ${member.side}, ${health}, ${armour}, ${conditions}`;}
