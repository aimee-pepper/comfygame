export const combatGraphProofVersion="combat-graph-proof-0.2.0";
export const offenseLanes=Object.freeze([
  Object.freeze({id:"force",name:"Force",nodes:Object.freeze(["Heavy Hand","Follow Through","Overbear","Bracing Stance","Stagger","Shatter","Momentum","Breaking Blow"])}),
  Object.freeze({id:"precision",name:"Precision",nodes:Object.freeze(["Keen Eye","Weak Point","Pry","Steady Hand","Exploit","Finish","Anatomy","Killing Stroke"])}),
  Object.freeze({id:"swiftness",name:"Swiftness",nodes:Object.freeze(["Quick Step","Light Touch","Quicken","Second Wind","Flurry","First Strike","Cascade","Blur"])})
]);
export function nodeID(lane,rank){if(!offenseLanes.some(x=>x.id===lane)||!Number.isInteger(rank)||rank<1||rank>8)throw new Error("invalid-combat-node");return`offense.${lane}.${rank}`;}
export function prerequisites(laneIndex,rank){if(!Number.isInteger(laneIndex)||laneIndex<0||laneIndex>2||!Number.isInteger(rank)||rank<1||rank>8)throw new Error("invalid-combat-position");if(rank===1)return Object.freeze({kind:"root",nodes:Object.freeze([])});const lane=offenseLanes[laneIndex].id;if(rank===4||rank===6){const prior=rank-1,indices=[laneIndex-1,laneIndex,laneIndex+1].filter(i=>i>=0&&i<3);return Object.freeze({kind:"any",nodes:Object.freeze(indices.map(i=>nodeID(offenseLanes[i].id,prior)))});}return Object.freeze({kind:"all",nodes:Object.freeze([nodeID(lane,rank-1)])});}
export function nodeRole(rank){return Object.freeze({isCapstone:rank===8,capstoneMinimumLaneSpend:rank===8?5:null});}
export function proofState(laneIndex,rank){const id=nodeID(offenseLanes[laneIndex].id,rank);if([nodeID("force",1),nodeID("force",2),nodeID("force",3),nodeID("precision",4)].includes(id))return"owned";if(id===nodeID("precision",5))return"selected";if(id===nodeID("force",4))return"available";return"blocked";}
export function prerequisiteText(laneIndex,rank){const p=prerequisites(laneIndex,rank);if(p.kind==="root")return"Root; no node prerequisite.";const names=p.nodes.map(id=>{const [,lane,r]=id.split("."),def=offenseLanes.find(x=>x.id===lane);return def.nodes[Number(r)-1];});let result=`Requires ${names.join(p.kind==="any"?" OR ":" AND ")}.`;if(rank===8)result+=` Also requires five owned nodes in ${offenseLanes[laneIndex].name}.`;return result;}
export function selectedDetail(){return Object.freeze({id:nodeID("precision",5),name:"Exploit",state:"selected",prerequisite:"Requires Steady Hand",reachability:"After learning Exploit, Killing Stroke remains reachable through Precision ranks 6, 7, and 8; the resulting five Precision nodes satisfy its commitment gate.",effect:"Add +4 raw damage when the target currently carries burn, poison, dazzle, or legacy bleed.",trigger:"Emanation without an affliction is insufficient.",action:"Learn Exploit"});}
