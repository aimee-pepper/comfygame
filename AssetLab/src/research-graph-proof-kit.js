export const researchGraphProofVersion="research-graph-proof-0.1.0",researchEvidenceRole="liveCurrentFixture";
export const externalPrerequisites=Object.freeze({tannery_keep_root:"Lined storage",tannery_carry_root:"Fitted expansion"});
const F=v=>Object.freeze(v);
export const holdNodes=F([
 ["shelving_one","Shelving",[]],["shelving_two","Shelving, deeper",["shelving_one"]],["shelving_three","Shelving, to the rafters",["shelving_two"]],["satchel_one","Satchel stitching",[]],["satchel_two","Satchel, reinforced",["satchel_one"]],["deepen_spring","Deepen the spring",[]],["shelving_four","Shelving, wall to wall",["shelving_three","tannery_keep_root"]],["shelving_five","A second room",["shelving_four"]],["shelving_six","The undercroft",["shelving_five"]],["shelving_seven","The undercroft, extended",["shelving_six"]],["shelving_eight","A hall for it",["shelving_seven"]],["shelving_nine","More than you can carry",["shelving_eight"]],["satchel_three","Satchel, deepened",["satchel_two","tannery_carry_root"]],["satchel_four","A second strap",["satchel_three"]],["satchel_five","Panniers",["satchel_four"]]
].map(([id,name,requires])=>F({id,name,requires:F(requires)})));
export const instrumentNodes=F(["Sunglass","Level","Thermoscope","Hygrometer","Loupe","Vivometer","Barometer","Chronometer"].map(name=>F({id:name.toLowerCase(),name,requires:F([]),rootGate:"survey_post_rules"})));
export function topologicalRanks(nodes,external=externalPrerequisites){const ids=new Set(),externalIDs=new Set(Object.keys(external));for(const node of nodes){if(externalIDs.has(node.id))throw Error(`research-external-node-collision:${node.id}`);if(ids.has(node.id))throw Error(`duplicate-research-node:${node.id}`);ids.add(node.id);if(new Set(node.requires).size!==node.requires.length)throw Error(`duplicate-research-prerequisite:${node.id}`);}const byID=new Map(nodes.map(n=>[n.id,n])),memo=new Map(),visiting=new Set();const rank=id=>{if(externalIDs.has(id))return 0;const node=byID.get(id);if(!node)throw Error(`dangling-research-prerequisite:${id}`);if(memo.has(id))return memo.get(id);if(visiting.has(id))throw Error(`research-cycle:${id}`);visiting.add(id);const value=node.requires.length?1+Math.max(...node.requires.map(rank)):0;visiting.delete(id);memo.set(id,value);return value;};return F(nodes.map(node=>F({id:node.id,rank:rank(node.id)})));}
export function researchProofPresentation(id){if(!holdNodes.some(n=>n.id===id))throw Error(`unknown-research-node:${id}`);const state=id==="shelving_one"||id==="satchel_one"?"completed":id==="shelving_two"?"selected":id==="satchel_two"?"available":id==="shelving_three"?"shortfall":"locked";const gateRole=id==="deepen_spring"?"springBranchCurrentLive":holdNodes.find(n=>n.id===id)?.requires.some(r=>r in externalPrerequisites)?"externalKeeperRequirement":"none";return F({state,gateRole});}
